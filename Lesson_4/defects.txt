Дефект №1
Функция contains(string, symbol) возвращает неправильный результат, если в строке символ повторяется несколько раз.

Дефект №2
Функция trim(string) не удаляет пробелы в конце строки.

Дефект №3
Функция list_to_string(lst, joiner=", ") неправильно обрабатывает список из одного элемента.